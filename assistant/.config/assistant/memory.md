## 2026-08-27 — The model I run on is a runtime choice, not an identity

Never answer "what model are you" from belief — ask the machine, in order of authority: (1) the panel footer, (2) ~/.local/state/quickshell/ori-model.json, (3) PI_MODEL/PI_PROVIDER in tool env. Verified via /proc PPID chain: one process, bash → pi → ori-agent → systemd. A model change applies only to the next cold spawn; PI_* env vars are pi's report about itself, not inherited environment.

## 2026-08-30 — Ori panel state: commands, effort, child limits

Panel commands (all panel-owned): /model /effort /new /compact /name /export /restart. /restart kills the pi CHILD via __kill — never the broker, which cannot be restarted from inside the panel (this connection lives in it). Shift+Tab cycles effort and arrives as Qt.Key_Backtab, never Key_Tab+ShiftModifier. Card border + composer edge show effort heat scale (off/minimal grey, low blue, medium yellow, high/xhigh red) via panel.effortColor(); anything written to `error` pins the border to the busy accent, so success feedback lives in a separate `notice` property; QML errors are invisible because quickshell's stdout/stderr go to /dev/null (Hyprland-spawned) — verify panel code with qmllint, never with logs. Warm pi re-reads nothing after boot: skills/commands/model lists refresh only on cold spawn (~10-min idle kill; a get_commands probe confirmed a warm child ignores new skills). A panel watcher to refresh the menu anyway was built and reverted — Netanel rejected it: a menu of commands the warm child cannot run is worse than a stale one; "ok to say not possible". set_thinking_level acks success:true for every string and clamps silently — trust only get_state or the thinking_level_changed event. pi's universal effort scale for cold panels: off/minimal/low/medium/high/xhigh.

2026-08-30 — Panel scroll/selection internals. BTT ListView coordinates (measured, qmltestrunner): newest message at contentY == -height, oldest at -contentHeight, both collapse to -height when content fits; the [-（ch-h), 0] TTB range is wrong. A Flickable (incl. ListView) with content SMALLER than its viewport stops delivering wheel events to children inside it — wheel faces must be siblings of the list, not children (bit both the transcript and the composer). An anchored item as a DIRECT child of a Column silently breaks the positioner's layout (all siblings stuck at y 0, no warning) — put such items inside the field they anchor to. Touchpad scrolls arrive as wheel events with pixelDelta at event rate: quantising angleDelta into notches makes them crawl; apply 1:1 with no animation. qmltestrunner harness: /usr/lib/qt6/bin/qmltestrunner -platform offscreen -input <file>; console.* output DOES print (as QDEBUG); singleton dirs need an explicit qmldir there (quickshell's implicit singleton registration is quickshell-only); QTest.mouseWheel/mousePress/mouseMove work for event simulation. Selection architecture: Selection.qml singleton (registry of TextEdits, anchor+head, visual y-order, Copy.take live) + SelectableArea mouse faces; anything not a registered TextEdit (collapsed batch tallies, tool rows) is skipped in multi-block copies by construction.
§
2026-09-04 — Voice: `speak` pi tool (extensions/speak.ts) — TTS via kokoro-npu, streams to speakers as the NPU generates (daemon keeps first batch <1s, 10x realtime). Defaults: bm_lewis, en-us; owner picked bm_lewis after hearing ef_dora/em_alex/hf_beta/hm_omega/am_michael. No bash wrapper needed.
